% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raster_cube_tables.R
\name{layervalues2pixel}
\alias{layervalues2pixel}
\title{Series of Pixel Values}
\usage{
layervalues2pixel(
  layer_values,
  main_tbl,
  col_name = "20220101",
  inline_output = TRUE,
  path_output = NULL,
  append = FALSE
)
}
\arguments{
\item{layer_values}{List. Values extracted per raster layer (from
\code{cube2table()}).}

\item{main_tbl}{A table with pixel metadata (e.g., from
\code{main_input_var()}), used to name each output table.}

\item{col_name}{Column name for each SWAT input table. Typically the first
date in the time series (e.g., "20220101").}

\item{inline_output}{Logical. If TRUE, returns a list of data.tables.}

\item{path_output}{Directory to write one file per pixel when
\code{inline_output = FALSE}.}

\item{append}{Logical. If TRUE, append to existing files; otherwise
overwrite.}
}
\value{
A list of tables (when \code{inline_output = TRUE}) or a set of files in
\code{path_output} (one for each pixel).
}
\description{
Converts layer-wise values from \code{cube2table()} into SWAT-style input:
a time series for each pixel in the study area.
}
\examples{
layer_values <- data.frame(
  ID = c(1, 2, 1, 2),
  values = c(10, 20, 11, 21),
  layer_name = c("day_1", "day_1", "day_2", "day_2")
)
main_tbl <- data.frame(NAME = c("tmin_1", "tmin_2"))
layervalues2pixel(
  layer_values = layer_values,
  main_tbl = main_tbl,
  col_name = "20200101"
)
}
